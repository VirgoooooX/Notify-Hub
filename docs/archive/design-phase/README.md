# 历史设计资料

本目录保存 Notify Hub 从立项到早期功能阶段的产品范围、架构、领域模型、专题方案和开发计划。

这些文件用于追溯设计背景，不再作为当前实现规范，也不要求随普通代码修改同步更新。当前开发约束以仓库根目录 `PROJECT_GUIDE.md`、代码、数据库迁移、OpenAPI 和测试为准；重要架构变化见 `docs/DECISIONS.md`，部署与恢复见 `docs/operations.md`。

归档文件保持原始编号和内容，便于通过 Git 历史和全文搜索定位早期决策。
